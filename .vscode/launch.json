{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd",
            "output": "${workspaceFolder}/cmd/__debug_stock_bot.exe",
            "cwd": "${workspaceFolder}/cmd"  // 改回 cmd 目錄
        }
    ]
}